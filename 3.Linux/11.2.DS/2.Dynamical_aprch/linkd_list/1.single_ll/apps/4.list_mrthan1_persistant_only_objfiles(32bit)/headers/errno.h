#ifndef ERRNO

#define ERRNO

int errno;

enum err{

	E1LIST=-8,
	ENFND=-7,
	ENOLIST=-6,
        EFOUND=-5,
        EMALL=-4,
        EEPTY=-3,
        EFULL,
        FAILURE,
        SUCCESS

};

#endif
