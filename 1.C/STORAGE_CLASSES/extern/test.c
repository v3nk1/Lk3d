#include<stdio.h>

int g=10;  //compiler will allocate memory for this g only..

main(){

	int k; //the memory will allocate for k at runtime in stack.., ie., when u run ./a.out then only the mem for k is allocated in stack..

}
